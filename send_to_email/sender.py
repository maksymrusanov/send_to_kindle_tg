import os
import smtplib


def send_email(sender_email, recevier_email, password):
    smtp_server = 'smtp.gmail.com'
    port = 587
    sender_email = sender_email
    recevier_email = recevier_email
    password = password
